<ResourceDictionary xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="using:Avalonia.Controls.Converters"
        xmlns:prototype="clr-namespace:Avalonia.Themes.Prototype"
        xmlns:theme="using:AngelSix.ThemeEngine.Generated"
        xmlns:colour="using:AngelSix.ThemeEngine.Colours"
        xmlns:size="using:AngelSix.ThemeEngine.Sizes"
        xmlns:icons="using:AngelSix.ThemeEngine.Icons">

  <Design.PreviewWith>
    <Border>
      <Border.Styles>
        <prototype:PrototypeTheme />
      </Border.Styles>

      <!-- Tree written once; each pane (light + dark) instantiates its own copy -->
      <prototype:LightDarkPreview Title="TreeViewItem States" Orientation="Horizontal">
        <DataTemplate>
          <!-- Multiple lets the two selected samples coexist; narrow tree fits a half-width pane -->
          <TreeView SelectionMode="Multiple" Width="260">
            <!-- Open item: collapse chevron, and hosts the state samples beneath -->
            <TreeViewItem Header="Open - expanded" IsExpanded="True">
              <!-- Closed item with a child: expand chevron; its leaf child hits :empty (no chevron) -->
              <TreeViewItem Header="Closed - has child">
                <TreeViewItem Header="Leaf - empty" />
              </TreeViewItem>
              <!-- Plain selected: accent-primary fill -->
              <TreeViewItem Header="Selected" IsSelected="True" />
              <!-- Forced hover: hover-background fill -->
              <TreeViewItem Header="Hovered" theme:Preview.State="Hover" />
              <!-- Disabled: flat chrome, dimmed text -->
              <TreeViewItem Header="Disabled" IsEnabled="False" />
              <!-- Disabled + selected together: surfaces the dedicated :disabled:selected fill -->
              <TreeViewItem Header="Disabled + Selected" IsEnabled="False" IsSelected="True" />
            </TreeViewItem>
          </TreeView>
        </DataTemplate>
      </prototype:LightDarkPreview>
    </Border>
  </Design.PreviewWith>

  <!-- Chevron sub-theme -->
  <ControlTheme x:Key="PrototypeTreeViewExpandCollapseChevron" TargetType="ToggleButton">
    <Setter Property="Margin" Value="0" />
    <Setter Property="Width" Value="{size:FontSizeSm}" />
    <Setter Property="Height" Value="{size:FontSizeSm}" />
    <Setter Property="Template">
      <ControlTemplate>
        <!-- The chevron pointing at the expanded direction -->
        <Border Background="Transparent"
                Width="{TemplateBinding Width}"
                Height="{TemplateBinding Height}"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
          <Path x:Name="ChevronPath"
                Data="{icons:ExpandChevron}"
                Stretch="Uniform"
                HorizontalAlignment="Center"
                VerticalAlignment="Center" />
        </Border>
      </ControlTemplate>
    </Setter>

    <Style Selector="^:checked /template/ Path#ChevronPath">
      <Setter Property="Data" Value="{icons:CollapseChevron}" />
    </Style>
  </ControlTheme>

  <ControlTheme x:Key="{x:Type TreeViewItem}" TargetType="TreeViewItem">

    <!-- Visual defaults -->
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="Foreground" Value="{colour:TextDefaultBrush}" />
    <Setter Property="BorderBrush" Value="Transparent" />
    <Setter Property="BorderThickness" Value="{size:ThicknessSm}" />
    <Setter Property="CornerRadius" Value="{size:RadiusSm}" />
    <Setter Property="Padding" Value="{size:Edges Horizontal={size:SpacingMd}, Vertical={size:SpacingSm}}" />
    <Setter Property="HorizontalAlignment" Value="{theme:ControlHorizontalAlignment}" />
    <Setter Property="VerticalAlignment" Value="{theme:ControlVerticalAlignment}" />
    <!-- HorizontalContentAlignment and VerticalContentAlignment not on TreeViewItem -->
    <Setter Property="MinHeight" Value="{size:ControlHeightSm}" />

    <!-- Template -->
    <Setter Property="Template">
      <ControlTemplate>
        <!-- Layout root stacking children under the item -->
        <StackPanel>
          <!-- Outer frame around the item row -->
          <Border Name="PART_LayoutRoot"
                  Focusable="True"
                  Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="{TemplateBinding CornerRadius}"
                  MinHeight="{TemplateBinding MinHeight}"
                  TemplatedControl.IsTemplateFocusTarget="True">
            <!-- Places the chevron left of the item's label -->
            <Grid Name="PART_Header"
                  ColumnDefinitions="Auto, *">
              <!-- Indented area holding the chevron -->
              <Panel Name="PART_ExpandCollapseChevronContainer">
                <!-- Expands and collapses this item's children -->
                <ToggleButton Name="PART_ExpandCollapseChevron"
                              Theme="{StaticResource PrototypeTreeViewExpandCollapseChevron}"
                              Focusable="False"
                              IsChecked="{TemplateBinding IsExpanded, Mode=TwoWay}" />
              </Panel>
              <!-- The item's label -->
              <ContentPresenter Name="PART_HeaderPresenter"
                                Grid.Column="1"
                                Focusable="False"
                                Background="Transparent"
                                Content="{TemplateBinding Header}"
                                ContentTemplate="{TemplateBinding HeaderTemplate}"
                                HorizontalAlignment="{TemplateBinding HorizontalAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalAlignment}"
                                Margin="{TemplateBinding Padding}" />
            </Grid>
          </Border>
          <!-- This item's child items -->
          <ItemsPresenter Name="PART_ItemsPresenter"
                          IsVisible="{TemplateBinding IsExpanded}"
                          ItemsPanel="{TemplateBinding ItemsPanel}" />
        </StackPanel>
      </ControlTemplate>
    </Setter>

    <!-- Hover state -->
    <Style Selector="^ /template/ Border#PART_LayoutRoot:pointerover, ^[(theme|Preview.State)=Hover] /template/ Border#PART_LayoutRoot">
      <Setter Property="Background" Value="{colour:HoverBackgroundDefaultBrush}" />
      <Setter Property="BorderBrush" Value="Transparent" />
    </Style>
    <Style Selector="^ /template/ Border#PART_LayoutRoot:pointerover > ContentPresenter#PART_HeaderPresenter, ^[(theme|Preview.State)=Hover] /template/ Border#PART_LayoutRoot > ContentPresenter#PART_HeaderPresenter">
    </Style>

    <!-- Pressed state -->
    <Style Selector="^:pressed /template/ Border#PART_LayoutRoot:pointerover, ^:pressed[(theme|Preview.State)=Pressed] /template/ Border#PART_LayoutRoot">
      <Setter Property="Background" Value="{colour:HoverBackgroundDefaultBrush}" />
      <Setter Property="BorderBrush" Value="Transparent" />
    </Style>
    <Style Selector="^:pressed /template/ Border#PART_LayoutRoot:pointerover > ContentPresenter#PART_HeaderPresenter, ^:pressed[(theme|Preview.State)=Pressed] /template/ Border#PART_LayoutRoot > ContentPresenter#PART_HeaderPresenter">
    </Style>

    <!-- Disabled state -->
    <Style Selector="^:disabled /template/ Border#PART_LayoutRoot">
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="BorderBrush" Value="Transparent" />
    </Style>
    <Style Selector="^:disabled /template/ ContentPresenter#PART_HeaderPresenter">
      <Setter Property="Foreground" Value="{colour:DimTextDefaultBrush}" />
    </Style>

    <!-- Selected state -->
    <Style Selector="^:selected /template/ Border#PART_LayoutRoot">
      <Setter Property="Background" Value="{colour:AccentPrimaryBrush}" />
      <Setter Property="BorderBrush" Value="Transparent" />
    </Style>
    <Style Selector="^:selected /template/ Border#PART_LayoutRoot > ContentPresenter#PART_HeaderPresenter">
    </Style>

    <!-- Selected hover state -->
    <Style Selector="^:selected /template/ Border#PART_LayoutRoot:pointerover, ^:selected[(theme|Preview.State)=Hover] /template/ Border#PART_LayoutRoot">
      <Setter Property="Background" Value="{colour:AccentPrimaryBrush}" />
      <Setter Property="BorderBrush" Value="Transparent" />
    </Style>
    <Style Selector="^:selected /template/ Border#PART_LayoutRoot:pointerover > ContentPresenter#PART_HeaderPresenter, ^:selected[(theme|Preview.State)=Hover] /template/ Border#PART_LayoutRoot > ContentPresenter#PART_HeaderPresenter">
    </Style>

    <!-- Selected pressed state -->
    <Style Selector="^:pressed:selected /template/ Border#PART_LayoutRoot:pointerover, ^:pressed:selected[(theme|Preview.State)=Pressed] /template/ Border#PART_LayoutRoot">
      <Setter Property="Background" Value="{colour:HoverBackgroundDefaultBrush}" />
      <Setter Property="BorderBrush" Value="Transparent" />
    </Style>
    <Style Selector="^:pressed:selected /template/ Border#PART_LayoutRoot:pointerover > ContentPresenter#PART_HeaderPresenter, ^:pressed:selected[(theme|Preview.State)=Pressed] /template/ Border#PART_LayoutRoot > ContentPresenter#PART_HeaderPresenter">
    </Style>

    <!-- Disabled selected state -->
    <Style Selector="^:disabled:selected /template/ Border#PART_LayoutRoot">
      <Setter Property="Background" Value="{colour:AccentBrush Light2}" />
      <Setter Property="BorderBrush" Value="Transparent" />
    </Style>
    <Style Selector="^:disabled:selected /template/ Border#PART_LayoutRoot > ContentPresenter#PART_HeaderPresenter">
      <Setter Property="Foreground" Value="{colour:DimTextDefaultBrush}" />
    </Style>

    <!-- Empty state -->
    <Style Selector="^:empty /template/ ToggleButton#PART_ExpandCollapseChevron">
      <Setter Property="IsVisible" Value="False" />
    </Style>
    <Style Selector="^:empty /template/ Panel#PART_ExpandCollapseChevronContainer">
      <Setter Property="Width" Value="{size:FontSizeSm}" />
    </Style>

    <!-- Themed properties moved out of ControlTemplate -->
    <Style Selector="^ /template/ Path#ChevronPath">
      <Setter Property="Fill" Value="{colour:AccentBrush Dark1}" />
    </Style>
    <Style Selector="^ /template/ Panel#PART_ExpandCollapseChevronContainer">
      <Setter Property="Margin" Value="{size:Edges Horizontal={size:SpacingXxl}}" />
    </Style>
  </ControlTheme>
</ResourceDictionary>
