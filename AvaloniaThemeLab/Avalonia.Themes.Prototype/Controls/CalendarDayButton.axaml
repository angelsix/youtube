<ResourceDictionary xmlns="https://github.com/avaloniaui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:prototype="clr-namespace:Avalonia.Themes.Prototype"
                    xmlns:theme="using:AngelSix.ThemeEngine.Generated">
  <Design.PreviewWith>
    <Border Padding="20">
      <Border.Styles>
        <prototype:PrototypeTheme />
      </Border.Styles>

      <StackPanel Spacing="20">
        <CalendarDayButton/>
      </StackPanel>
    </Border>
  </Design.PreviewWith>

  <!-- ============================================================
       ControlTheme: {x:Type CalendarDayButton}
       ============================================================ -->
  <ControlTheme x:Key="{x:Type CalendarDayButton}" TargetType="CalendarDayButton">

    <!-- Visual defaults -->
    <Setter Property="ClickMode" Value="Release" />
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="Foreground" Value="{theme:AccentBrush Dark1}" />
    <Setter Property="BorderBrush" Value="Transparent" />
    <Setter Property="BorderThickness" Value="{theme:ThicknessMd}" />
    <Setter Property="CornerRadius" Value="{theme:RadiusSm}" />
    <Setter Property="Padding" Value="0" />
    <Setter Property="HorizontalAlignment" Value="{theme:ControlHorizontalAlignment}" />
    <Setter Property="VerticalAlignment" Value="{theme:ControlVerticalAlignment}" />
    <Setter Property="HorizontalContentAlignment" Value="{theme:ControlHorizontalContentAlignment}" />
    <Setter Property="VerticalContentAlignment" Value="{theme:ControlVerticalContentAlignment}" />
    <Setter Property="MinWidth" Value="{theme:ControlHeightMd}" />
    <Setter Property="MinHeight" Value="{theme:ControlHeightMd}" />
    <Setter Property="Margin" Value="{theme:ThicknessSm}" />
    <Setter Property="ClipToBounds" Value="False" />

    <!-- Template -->
    <Setter Property="Template">
      <ControlTemplate>
        <!-- Layout root stacking the fill and its outline -->
        <Panel>
          <!-- Filled background of the day cell -->
          <Border Name="Root"
                  Background="{TemplateBinding Background}"
                  BorderThickness="0"
                  ClipToBounds="True">
            <!-- The day number -->
            <ContentPresenter Name="PART_ContentPresenter"
                              Content="{TemplateBinding Content}"
                              ContentTemplate="{TemplateBinding ContentTemplate}"
                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                              Margin="{TemplateBinding Padding}" />
          </Border>
          <!-- Outline drawn over the fill, for selection and hover -->
          <Border Name="Border"
                  BorderBrush="{TemplateBinding BorderBrush}" />
        </Panel>
      </ControlTemplate>
    </Setter>

    <!-- Default border thickness -->
    <Style Selector="^ /template/ Border#Border">
      <Setter Property="BorderThickness" Value="{theme:ThicknessMd}" />
    </Style>

    <!-- Hover state -->
    <Style Selector="^:pointerover /template/ Border#Border">
      <Setter Property="BorderBrush" Value="{theme:AccentBorderBrush}" />
    </Style>

    <!-- Pressed state -->
    <Style Selector="^:pressed /template/ Border#Border">
      <Setter Property="BorderBrush" Value="{theme:AccentBorderBrush}" />
    </Style>

    <!-- Disabled state -->
    <Style Selector="^:disabled /template/ ContentPresenter#PART_ContentPresenter">
      <Setter Property="Foreground" Value="{theme:AccentBrush Light2}" />
    </Style>

    <!-- Selected state -->
    <Style Selector="^:selected">
      <Style Selector="^ /template/ Border#Border">
        <Setter Property="BorderBrush" Value="{theme:AccentPrimaryBrush}" />
      </Style>
      <Style Selector="^:pointerover /template/ Border#Border">
        <Setter Property="BorderBrush" Value="{theme:AccentPrimaryBrush}" />
      </Style>
      <Style Selector="^:pressed /template/ Border#Border">
        <Setter Property="BorderBrush" Value="{theme:AccentPrimaryBrush}" />
      </Style>
    </Style>

    <!-- Today state -->
    <Style Selector="^:today">
      <Style Selector="^ /template/ Border#Root">
        <Setter Property="Background" Value="{theme:AccentPrimaryBrush}" />
      </Style>
      <Style Selector="^ /template/ ContentPresenter#PART_ContentPresenter">
        <Setter Property="Foreground" Value="{theme:AccentBrush Light10}" />
        <Setter Property="FontWeight" Value="{theme:FontWeightSemiBold}" />
      </Style>
      <Style Selector="^:pointerover /template/ Border#Border">
        <Setter Property="BorderBrush" Value="{theme:AccentBrush Dark1}" />
      </Style>
      <Style Selector="^:pressed /template/ Border#Border">
        <Setter Property="BorderBrush" Value="{theme:AccentBrush Dark2}" />
      </Style>
    </Style>

    <!-- Inactive state -->
    <Style Selector="^:inactive">
      <Style Selector="^ /template/ Border#Root">
        <Setter Property="Background" Value="{theme:AccentBrush Light2}" />
      </Style>
      <Style Selector="^ /template/ ContentPresenter#PART_ContentPresenter">
      </Style>
    </Style>

    <!-- Blackout state -->
    <Style Selector="^:blackout /template/ ContentPresenter#PART_ContentPresenter">
      <Setter Property="Foreground" Value="{theme:AccentBrush Light2}" />
    </Style>

  </ControlTheme>
</ResourceDictionary>
