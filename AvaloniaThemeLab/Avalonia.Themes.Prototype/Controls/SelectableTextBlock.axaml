<ResourceDictionary xmlns="https://github.com/avaloniaui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:theme="using:AngelSix.ThemeEngine.Generated"
                    x:ClassModifier="internal">

  <!-- ============================================================
       ControlTheme: {x:Type SelectableTextBlock}
       ============================================================ -->
  <ControlTheme x:Key="{x:Type SelectableTextBlock}" TargetType="SelectableTextBlock">

    <!-- Visual defaults -->
    <Setter Property="Foreground" Value="{theme:OnSurfaceBrush}" />
    <Setter Property="FontFamily" Value="{theme:FontFamily}" />
    <Setter Property="FontSize" Value="{theme:FontSizeMd}" />
    <Setter Property="FontWeight" Value="{theme:FontWeightRegular}" />
    <Setter Property="Background" Value="{theme:SurfaceBrush}" />
    <Setter Property="Padding" Value="{theme:SpacingMd}" />
    <Setter Property="HorizontalAlignment" Value="{theme:ControlHorizontalAlignment}" />
    <Setter Property="VerticalAlignment" Value="{theme:ControlVerticalAlignment}" />
    <Setter Property="SelectionBrush" Value="{theme:AccentPrimaryBrush}" />

    <!-- Enabled state -->
    <Style Selector="^[IsEnabled=True]">
      <Setter Property="Cursor" Value="IBeam" />
      <Setter Property="ContextFlyout" Value="{StaticResource SelectableTextBlockContextFlyout}" />
    </Style>
  </ControlTheme>

  <!-- Context flyout -->
  <MenuFlyout x:Key="SelectableTextBlockContextFlyout" Placement="Bottom">
    <MenuItem Header="Copy" Command="{Binding $parent[SelectableTextBlock].Copy}"
              IsEnabled="{Binding $parent[SelectableTextBlock].CanCopy}" InputGesture="{x:Static TextBox.CopyGesture}" />
  </MenuFlyout>
</ResourceDictionary>
