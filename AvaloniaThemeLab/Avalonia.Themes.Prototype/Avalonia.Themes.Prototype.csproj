<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>net10.0</TargetFramework>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
        <RootNamespace>Avalonia.Themes.Prototype</RootNamespace>
    </PropertyGroup>

    <ItemGroup>
        <PackageReference Include="Avalonia" Version="12.0.1"/>
        <PackageReference Include="AngelSix.ThemeEngine" Version="1.5.0" />
    </ItemGroup>

    <!-- Keeps StyleClasses.axaml mirroring the ControlThemes. See the .targets file. -->
    <Import Project="StyleClasses.targets" />

</Project>
